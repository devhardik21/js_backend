#include<stdio.h>
int main ()
{
    int x,y;
    printf("put the value of x:");
    scanf("%d",&x);
     printf("put the value of y:");
      scanf("%d",&y);
      int sum= x+y;
      printf("the sum of the two numbers are: %d",sum);
      return 0 ;
    
}