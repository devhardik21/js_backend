#include<stdio.h>
int main ()
{
    int travis,_21savage,drake;
   printf("age of trav");
   scanf("%d",&travis);
   printf("age of 21");
   scanf("%d",&_21savage);
   printf("age of drake");
   scanf("%d",&drake);
   if(drake<travis && drake< _21savage)
   {
    printf("drake is smaller than both");
   }
   if(travis<_21savage && travis<drake);
   {
    printf("travis is smaller than both");
   }
if(_21savage<travis && drake> _21savage)
   {
    printf("21 savage is smaller than both");
   }
   return 0;
}